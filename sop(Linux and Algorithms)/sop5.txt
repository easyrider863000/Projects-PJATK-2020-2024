1--------------------------------------------------------
ls -a $1 | grep -E '^\..*b+.*'
---------------------------------------------------------
2--------------------------------------------------------
ls | grep -E '^[bes]'
---------------------------------------------------------
3--------------------------------------------------------
K=$1
P=$2
ls -al $K | cut -s -d' ' -f6 >> $P.txt
---------------------------------------------------------
4--------------------------------------------------------
K=$1
P=$2
ls -al $K | cut -s -d' ' -f6 >> $P.txt || echo "Error"
---------------------------------------------------------
5--------------------------------------------------------
ls -d /$1/*/ | wc -l
---------------------------------------------------------
6--------------------------------------------------------
ls $1/ | wc -l
ls $2/ | wc -l
---------------------------------------------------------
7--------------------------------------------------------
cat /etc/passwd | cut -d ':' -f1,6
---------------------------------------------------------
8--------------------------------------------------------
echo $PATH | tr -d ':' | tr -d '/' | wc -c
---------------------------------------------------------
9--------------------------------------------------------
UZYTKOWNIK="root"
UZYTLOG=$(who | grep $UZYTKOWNIK | wc -l)
echo $UZYTLOG
---------------------------------------------------------
10-------------------------------------------------------
UZYTKOWNIK="root"
while [ 1 -eq 1 ] 
do 
UZYTLOG=$(who | grep $UZYTKOWNIK | wc -l)
if [ $UZYTLOG -eq 3 ]
exit 0
fi
done
---------------------------------------------------------
11-------------------------------------------------------
head -n-20 $1 | tail -n1
---------------------------------------------------------
12-------------------------------------------------------
wst=$(cat $3 | wc -l)+1
let M=$wst-$1
let N=$2-$1+1
tail -n$M $3 |head -n$N
---------------------------------------------------------
13-------------------------------------------------------
wst=$(cat $1 | wc -l)
let srodek=$wst/2
head -n$srodek $1 | tail -n1
---------------------------------------------------------
